# Draft Gerber Package Notes

These files are hand-authored placeholders so the submission package has the expected `production/gerbers.zip` path.

Before final Hackpad submission:

1. Open `PCB/egepad.kicad_pro` in KiCad.
2. Run ERC/DRC.
3. Export real Gerbers and drill files from KiCad.
4. Replace this folder and `production/gerbers.zip`.

